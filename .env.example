# Simple Share 环境变量配置模板
# 
# 使用方法：
# 1. 复制此文件为 .env: cp .env.example .env
# 2. 填写下面的环境变量值
# 3. 注意：.env 文件已被 .gitignore 忽略，不会提交到 Git
#
# ⚠️ 重要提示：
# - 本地开发：可以使用此 .env 文件
# - 生产环境：必须在 Cloudflare Dashboard 中配置环境变量
#   路径：Workers & Pages → 你的 Worker → Settings → Variables → Environment Variables
#   确保选择 Production 环境（不是 Preview）

# ============================================
# 必需配置
# ============================================

# JWT 签名密钥（至少 32 字符）
# 生成方式：openssl rand -hex 32
JWT_SECRET=dev-jwt-secret-change-in-production-min-32-chars

# R2 公共访问 URL（可选，如果使用 R2 公共访问）
R2_PUBLIC_URL=https://your-r2-domain.com

# D1 数据库 ID（可选，如果设置了，部署脚本会自动配置绑定）
# 如果未设置，使用 Cloudflare Dashboard 绑定（推荐）
D1_DATABASE_ID=

# ============================================
# 说明
# ============================================
# 
# 1. 本地开发：
#    - 复制此文件为 .env
#    - 填写本地开发所需的环境变量
#    - 使用 wrangler dev 时会自动读取这些变量
#
# 2. 生产环境：
#    - 必须在 Cloudflare Dashboard 中配置
#    - 路径：Workers & Pages → simple-share → Settings → Variables → Environment Variables
#    - 确保选择 Production 环境
#    - 配置后会自动持久化，每次部署都会保留
#
# 3. 安全提示：
#    - 不要将包含真实值的 .env 文件提交到 Git
#    - .env 文件已在 .gitignore 中，不会被提交
#    - 生产环境的敏感信息必须在 Dashboard 中配置
#
# 详细配置说明请查看：ENV_SETUP.md
